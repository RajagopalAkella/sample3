public without sharing class deleteExample2 {
    public static void callMethod() {
        integer z;
        z = deleteExample1.sumNumbers(1,2);
        system.debug('Sum of 1 and 2 is: ' + z);
    }
}